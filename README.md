This library create one file for each table on a database.
Those classes allow simple database manipulation with almost none SQL.

## Instalation

```bash
python -m pip install --index-url https://test.pypi.org/simple/ --no-deps BootstrapDB-armandokeller
````
